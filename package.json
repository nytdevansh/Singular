{
  "name": "singular-framework",
  "private": false,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "pnpm --filter counter-app dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "tslib": "^2.8.1"
  }
}